<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:background="#FCF7F7"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:id="@+id/suggestions_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Suggestions and Feedback"
        android:textSize="25sp"
        android:layout_margin="20dp"/>

    <android.support.v7.widget.CardView
        android:id="@+id/parentcv"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        card_view:cardBackgroundColor="#FDFDFD"
        card_view:cardCornerRadius="5dp"
        card_view:cardElevation="2dp"
        android:layout_marginLeft="12dp"
        android:layout_marginRight="12dp"
        android:layout_below="@id/suggestions_tv">

        <EditText
            android:id="@+id/suggestions_et"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_margin="25dp"/>

    </android.support.v7.widget.CardView>

    <Button
        android:id="@+id/suggestions_bt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Submit"
        android:layout_marginTop="20dp"
        android:layout_below="@id/parentcv"/>
</RelativeLayout>